# mvm-engine
Parameterizable, pipelined SystemVerilog matrix-vector multiplication engine with configurable parallel compute lanes, inspired by Microsoft's BrainWave NPU architecture. Meets timing at ~380 MHz on FPGA.
